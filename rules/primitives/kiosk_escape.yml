id: PRIM-KIOSK-ESCAPE
name: kiosk_escape_via_keyboard
category: kiosk_escape
environment:
  os: windows
  mode: kiosk
prerequisites:
  - keyboard_limited
technique:
  - allowed_key_combo
outcome:
  - arbitrary_execution
